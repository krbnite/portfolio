# Example page registry (sanitized)
# Maps human-readable page names to Facebook Page IDs.
# In production, this was populated from a live account listing
# via fb_token.accounts (see api/graph_api.py).
page_name_to_id = {
    'page_01': 'page_id_01',
    'page_02': 'page_id_02',
    'page_03': 'page_id_03',
}
